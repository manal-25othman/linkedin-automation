/**
 * دعوة الاستبيان داخل المنصة.
 *
 * تُقرأ بمرجع صريح لا ديناميكي: Next.js تستبدل متغيرات NEXT_PUBLIC_ وقت
 * البناء، والقراءة الديناميكية تعود فارغة في المتصفح.
 *
 * إن لم يُضبط الرابط تُخفى الدعوة كلها — دعوة بلا رابط أسوأ من غيابها.
 * ويُقبل https فقط: الرابط يُفتح من داخل صفحة مسجَّل فيها الدخول، ولا
 * يليق أن يُحال المستخدم منها إلى شيء غير مؤمَّن أو إلى مخطّط غريب.
 */

const RAW_SURVEY_URL = (process.env.NEXT_PUBLIC_FEEDBACK_SURVEY_URL ?? '').trim();

export const FEEDBACK_SURVEY_URL = /^https:\/\/[^\s]+$/i.test(RAW_SURVEY_URL) ? RAW_SURVEY_URL : '';

/**
 * عدد الإجابات التي يراها المستخدم قبل أن تظهر الدعوة.
 *
 * خمس إجابات فعلية (لا أسئلة بلا جواب) — من رأى المساعد يجيب خمس مرات
 * كوّن رأيًا يستحق أن يُسأل عنه؛ وقبلها الدعوة إزعاج لمن لم يجرّب بعد.
 */
export const FEEDBACK_SURVEY_MIN_ANSWERS = 5;

/** مفتاح الرفض في المتصفح — تُخفى الدعوة بعده ولا تعود */
export const FEEDBACK_SURVEY_DISMISSED_KEY = 'maarifah:survey-dismissed';
