export interface FaqItem {
  question: string;
  answer: string;
}

/**
 * الأسئلة نفسها لم تعد هنا.
 *
 * كانت مصفوفتين مكتوبتين في هذا الملف، فتغيير سؤال واحد يحتاج مطوّرًا
 * ونشرًا. وهي الآن مدخل `faq.items` في سجلّ المحتوى، تُحرَّر من لوحة
 * المنصة، وتُقرأ عبر `faqGroups()` و`homeFaq()` في `@/lib/content/faq`.
 *
 * وبقي هذا النوع لأن `FaqList` يستهلكه، ولأن شكل السؤال والجواب ثابت
 * مهما تغيّر مصدره.
 */
