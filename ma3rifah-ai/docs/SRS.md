# وثيقة متطلبات البرمجيات (SRS)

**المنتج:** معرفة AI — Ma3rifah AI
**الوصف:** منصة SaaS لإدارة المعرفة المؤسسية بالذكاء الاصطناعي، عربية أولًا
**الإصدار:** 1.3 — ما قبل الإطلاق التجاري
**التاريخ:** 2026-08-16 (محدَّثة بالفوترة والدفع، والتنبيهات، وواتساب، والدعم، وحدود الخطط، والمراجعة الأمنية)

> هذه الوثيقة تصف **النظام كما هو مبنيّ فعلًا**، لا كما نتمنّاه. كل رقم
> فيها مأخوذ من الكود أو من الهجرات. وما لم يُبنَ بعد مذكور صراحةً في
> القسم ١٠ «خارج النطاق الحالي» — لأن وثيقة تخلط المنجز بالمأمول تضلّل
> صاحبها قبل أن تضلّل غيره.

---

## ١. المقدمة

### ١.١ الغرض

تحديد المتطلبات الوظيفية وغير الوظيفية لمنصة «معرفة AI»، لتكون مرجعًا
للتطوير والاختبار والتفاوض مع العملاء والشركاء.

### ١.٢ نطاق المنتج

منصة سحابية متعددة المستأجرين (Multi-tenant SaaS) تمكّن الشركة من رفع
مستنداتها الداخلية، فتتحوّل تلقائيًا إلى قاعدة معرفة قابلة للسؤال
بالعربية. يسأل الموظف سؤالًا بلغته الطبيعية، فيجيبه مساعد ذكي بإجابة
**مستندة إلى وثائق شركته وحدها**، مع ذكر المصدر.

**المشكلة التي يحلّها:** المعرفة في الشركات السعودية محبوسة في ملفات
متناثرة وفي رؤوس الموظفين. الموظف الجديد يسأل زميله، والزميل مشغول،
والجواب يتأخر أو يأتي خاطئًا. وحين يستقيل موظف، تخرج معرفته معه.

### ١.٣ التعريفات

| المصطلح | المعنى |
|---|---|
| **المستأجر (Tenant)** | شركة عميلة. كل بياناتها معزولة عزلًا تامًا |
| **RAG** | الاسترجاع المعزّز بالتوليد: يبحث في وثائق الشركة ثم يجيب منها |
| **المقطع (Chunk)** | جزء من مستند بحجم مناسب للبحث الدلالي |
| **التضمين (Embedding)** | تمثيل رقمي للنص يتيح البحث بالمعنى لا بالكلمة |
| **RLS** | أمن مستوى الصف في PostgreSQL — حدّ العزل الفعلي |
| **فجوة معرفية** | سؤال لم تجب عنه قاعدة المعرفة، يدلّ على نقص في التوثيق |
| **إجابة معتمدة** | جواب يكتبه مدير الشركة لسدّ فجوة، يُفهرس مستندًا فيجده البحث |
| **حقن التعليمات** | نصّ داخل مستند يحاول أن يُقرأ أمرًا للنموذج لا بيانات |
| **الهللة** | جزء الريال من مئة — وحدة المبالغ لدى بوابة الدفع، صحيحة بلا كسور |
| **مفتاح الخدمة** | مفتاح Supabase يتجاوز RLS؛ لا يُستعمل إلا بعد تحقّق صريح في الكود |

### ١.٤ المراجع

- `docs/PHASE-12-SECURITY-VERIFICATION.md` — تقرير التحقق الأمني
- `docs/SETUP-CHECKLIST.md` — دليل الضبط التشغيلي خطوة بخطوة
- `docs/OWNER-ACCOUNT.md` — حساب مالك المنصة
- `docs/WHATSAPP-SETUP.md` — ربط واتساب
- `supabase/migrations/` — مخطط قاعدة البيانات (٢١ ترحيلًا)
- `supabase/ALL_MIGRATIONS.sql` — الترحيلات مجمَّعة، آمنة لإعادة التشغيل
- `tests/sql/` — اختبارات العزل بين المستأجرين (٩٩ اختبارًا)
- `scripts/prompt-injection-drill.ts` — مختبر حقن التعليمات على النموذج الحيّ

---

## ٢. الوصف العام

### ٢.١ منظور المنتج

منتج قائم بذاته، يُقدَّم عبر المتصفح باشتراك شهري. لا يتطلب تثبيتًا ولا
بنية تحتية لدى العميل.

### ٢.٢ فئات المستخدمين

| الدور | الوصف | الصلاحيات الجوهرية |
|---|---|---|
| **SUPER_ADMIN** | مالك المنصة | إدارة المنصة والشركات والخطط. لا يقرأ محتوى وثائق العملاء |
| **COMPANY_ADMIN** | مدير الشركة العميلة | إدارة الشركة والمستخدمين والأقسام والمستندات والتحليلات والفوترة |
| **MANAGER** | مدير قسم | عرض المستخدمين والأقسام، تحليلات قسمه، استخدام المساعد |
| **EMPLOYEE** | موظف | استخدام المساعد، عرض المستندات المصرّح له بها، محادثاته وحده |

### ٢.٣ بيئة التشغيل

| الطبقة | التقنية |
|---|---|
| الواجهة والخادم | Next.js 15 (App Router) + React 19 + TypeScript |
| التنسيق | Tailwind CSS + Radix UI — RTL أصلي |
| قاعدة البيانات | PostgreSQL (Supabase) + امتداد pgvector |
| المصادقة | Supabase Auth |
| التخزين | Supabase Storage |
| التوليد | Claude — `claude-opus-5` للمساعد، `claude-haiku-4-5` للمهام الخفيفة |
| التضمينات | Voyage AI — `voyage-3.5`، أبعاد ١٠٢٤ |
| الاستضافة | Vercel |
| البريد | Resend عبر SMTP في Supabase |
| بوابة الدفع | Moyasar — بالريال، تدعم مدى وApple Pay |
| واتساب | Meta WhatsApp Cloud API |

### ٢.٤ القيود

- **العربية أولًا:** الواجهة RTL بالكامل، والمعالجة النصية تراعي خصائص العربية
- **العزل مطلق:** لا مسار برمجي يسمح لشركة برؤية بيانات أخرى
- **بلا بيانات وهمية في الوظائف الجوهرية:** الإجابات من وثائق حقيقية أو لا إجابة

### ٢.٥ الافتراضات

- العميل يملك وثائق نصية (لا صورًا ممسوحة) — فالنظام لا يدعم OCR بعد
- اتصال إنترنت مستقر
- الشركة مستعدة لرفع وثائقها الداخلية إلى سحابة خارجية

---

## ٣. المتطلبات الوظيفية

### ٣.١ الحسابات والمصادقة

| الرقم | المتطلب | الحالة |
|---|---|---|
| FR-1.1 | تسجيل شركة جديدة بإنشاء حساب مدير لها | ✅ مبنيّ |
| FR-1.2 | تسجيل الدخول والخروج | ✅ مبنيّ |
| FR-1.3 | إنشاء ملف الشركة تلقائيًا عند التسجيل وربط المدير بها | ✅ مبنيّ |
| FR-1.4 | تعديل الملف الشخصي | ✅ مبنيّ |
| FR-1.5 | دعوة موظف بالبريد الإلكتروني | ✅ مبنيّ — يصل الموظفَ رابطٌ يضع به كلمة مروره بنفسه |
| FR-1.6 | **كلمة المرور لا تمرّ بالمدير** | الحساب يُنشأ بكلمة عشوائية لا تُعرض ولا تُسجَّل؛ ولا تظهر للمدير إلا إن تعذّر إرسال البريد |
| FR-1.7 | إعادة إرسال رابط الدخول | زر في جدول المستخدمين — الرسالة الأولى تضيع كثيرًا |
| FR-1.8 | استعادة كلمة المرور | عبر `/auth/callback` الذي يبدّل رمز PKCE بجلسة |
| FR-1.9 | تفعيل المدعو تلقائيًا | عند فتح الرابط، لا عند تسجيل الدخول وحده |
| FR-1.10 | حجب الحساب المعطّل من رابط البريد | من يملك البريد يطلب رابطًا متى شاء؛ بلا فحص يصير التفافًا على التعطيل |

### ٣.٢ إدارة المستندات

| الرقم | المتطلب | التفصيل |
|---|---|---|
| FR-2.1 | رفع مستند | الأنواع: PDF, DOCX, XLSX, XLS, TXT, MD, CSV — حتى ٢٥ ميجابايت |
| FR-2.2 | استخراج النص تلقائيًا | معالجة خاصة للعربية: ترتيب منطقي، فكّ روابط لام-ألف، استنتاج المسافات |
| FR-2.3 | التقطيع | مستهدف ١٢٠٠ محرف، أقصى ١٨٠٠، أدنى ١٢٠، تداخل ١٨٠ محرفًا |
| FR-2.4 | توليد التضمينات وتخزينها | `vector(1024)` بفهرس HNSW ومسافة جيب التمام |
| FR-2.5 | منع تكرار الملف | بصمة SHA-256 لكل ملف داخل الشركة |
| FR-2.6 | حالة المعالجة الظاهرة | PROCESSING / READY / FAILED مع **سبب الفشل** لا رسالة عامة |
| FR-2.7 | إعادة المعالجة | زر يعيد الاستخراج والتقطيع والتضمين |
| FR-2.8 | التصنيف | تصنيفات معرفية تُعرّفها الشركة |
| FR-2.9 | تقييد الوصول | على مستوى الشركة كاملة، أو أقسام محدّدة، أو أدوار محدّدة |
| FR-2.10 | **فرض حدّ المستندات** | يُفحص قبل الرفع؛ المؤرشف لا يُحتسب |
| FR-2.11 | **فرض حدّ التخزين** | يُفحص بحجم الملف القادم قبل رفعه لا بعده |
| FR-2.12 | اسم تخزين ASCII | الاسم العربي يُحفظ في قاعدة البيانات للعرض، ومفتاح التخزين ASCII لأن Supabase ترفض غيره |

### ٣.٣ المساعد الذكي (RAG)

| الرقم | المتطلب | التفصيل |
|---|---|---|
| FR-3.1 | سؤال بالعربية الطبيعية | مع دعم الإنجليزية |
| FR-3.2 | بحث دلالي في وثائق الشركة | مقيّد بالشركة **وبصلاحية المستخدم على كل مستند** |
| FR-3.3 | إجابة مستندة إلى المصادر | سقف سياق ٦٠٠٠ رمز |
| FR-3.4 | ذكر المصادر | اسم المستند ورقم الصفحة حين تتوفّر |
| FR-3.5 | الاعتراف بعدم المعرفة | لا يخترع إجابة حين لا يجد مصدرًا — تُسجَّل فجوة معرفية |
| FR-3.6 | محادثات محفوظة | بعنوان يُولَّد تلقائيًا |
| FR-3.7 | خصوصية المحادثة | لا يرى الموظف محادثات غيره |
| FR-3.8 | ضبط التكلفة | نافذة تاريخ محدودة، سقف رموز، تخزين مؤقت لموجّه النظام |

### ٣.٣.١ التحقق من المصادر ومكافحة الهلوسة

| الرقم | المتطلب | التفصيل |
|---|---|---|
| FR-3.9 | اختيار سياق بعتبة نسبية | `τ = max(τ_مطلقة، 0.75 × أعلى_تشابه)` — يمنع تسرّب مقاطع ضعيفة الصلة تُغري النموذج بالحشو |
| FR-3.10 | تنويع السياق (MMR) | `MMR(c) = 0.7·تشابه − 0.3·أقصى_تكرار` — يمنع إهدار السياق في نسخ مكرّرة |
| FR-3.11 | استشهاد صريح إلزامي | يلتزم النموذج بوسم كل جملة بـ`[مصدر ن]`، وتُحذف الوسوم قبل العرض |
| FR-3.12 | عرض المصادر المستعملة فقط | لا تُعرض كل المقاطع المسترجعة — عرضها ادّعاء رسوخ لم يحدث |
| FR-3.13 | التحقق من الأرقام | كل رقم في الإجابة يجب أن يرد في المصادر، وإلا ظهر تحذير يسمّيه |
| FR-3.14 | درجة الرسوخ | `G = |رموز الإجابة ∩ رموز المصادر| ÷ |رموز الإجابة|` بعد تطبيع عربي |
| FR-3.15 | درجة ثقة مركّبة | متوسط مرجَّح على الأدلة المتوفرة: `0.25·تشابه + 0.45·رسوخ + 0.30·أرقام` |
| FR-3.16 | عرض الثقة للموظف | شارة «عالية / متوسطة / منخفضة» بجانب المصادر |
| FR-3.17 | قياس الجودة عبر الزمن | تُخزَّن الدرجات فتظهر في التحليلات كإنذار مبكر |

### ٣.٣.٢ الحماية من حقن التعليمات

محتوى المستند مدخَل **غير موثوق**: قد يكون مرفقًا وصل بالبريد أو ملفًا
من مورّد. ويمرّ إلى النموذج كما يمرّ مدخَل المستخدم إلى قاعدة البيانات.

| الرقم | المتطلب | التفصيل |
|---|---|---|
| FR-3.18 | نصّ صريح: المقاطع بيانات لا أوامر | مع أمثلة للتوجيهات المزروعة وأمرٍ بعدم تنفيذها والإشارة إليها |
| FR-3.19 | حدود صريحة حول كل مقطع وحول السؤال | `<<<محتوى>>> … <<<نهاية المحتوى>>>` |
| FR-3.20 | تحييد علامة المصدر المزوّرة | `[مصدر 3]` داخل المتن ← `(مصدر 3)`؛ الإسناد المزيّف يوقف المراجعة فهو أسوأ من غيابه |
| FR-3.21 | تحييد علامة الحدّ المنتحَلة | تمنع إنهاء منطقة البيانات مبكرًا فيصير ما بعدها تعليمات |
| FR-3.22 | تحييد عناوين الأقسام | تمنع الإيهام ببداية قسم جديد في الرسالة |
| FR-3.23 | منع إفشاء الموجّه وذكر مستندات خارج القائمة | — |

**حدّ الضرر:** الحقن **لا يعبر الشركات**. الاسترجاع يُصفّى في قاعدة
البيانات قبل أن يرى النموذج شيئًا، فلا يستطيع مستند في شركة أن يستحضر
مستند شركة أخرى مهما بلغت براعة صياغته. والضرر المتبقي داخل الشركة
الواحدة: تزوير الإسناد، وتعطيل قواعد عدم الاختلاق، وتوجيه الموظف إلى
إجراء خاطئ.

**التحييد يكسر الشكل ولا يحذف**، فيبقى المعنى مقروءًا — الحذف يفقد
معلومة قد تكون مقصودة في المستند أصلًا.

### ٣.٤ التحليلات

| الرقم | المتطلب |
|---|---|
| FR-4.1 | الاستخدام والنشاط: عدد الأسئلة، المستخدمون النشطون، النشاط بالساعة |
| FR-4.2 | فجوات المعرفة: الأسئلة بلا إجابة وتوصيات بما ينقص توثيقه |
| FR-4.6 | **الإجابة المعتمدة**: يكتب المدير جوابًا فيدخل قاعدة المعرفة مستندًا يجده البحث |
| FR-4.7 | تسجيل من سأل كل فجوة، لتنبيهه عند حلّها |
| FR-4.8 | **التنبيهات داخل المنصة**: سؤالك صار له جواب، فجوة جديدة، فشل معالجة مستند |
| FR-4.3 | جودة الإجابات: نسبة المُجاب عنها، زمن الاستجابة، متوسط الرسوخ، عدد الإجابات ذات أرقام غير مؤكدة |
| FR-4.4 | التكلفة والاستهلاك: الرموز المستهلكة وتقدير التكلفة |
| FR-4.5 | إحصاءات الزوّار: عدد الزوّار وأسئلتهم غير المُجابة |

### ٣.٥ محادثة الزوّار (تسويقية)

| الرقم | المتطلب |
|---|---|
| FR-5.1 | مساعد للزائر المجهول يجيب عن المنصة نفسها |
| FR-5.2 | **مصدره نص ثابت في الكود** — لا يلمس أي بيانات شركة إطلاقًا |
| FR-5.3 | حد ٦ رسائل/دقيقة و٤٠ رسالة لكل زائر |
| FR-5.4 | تسجيل الأسئلة غير المُجابة كمؤشر على ما يجهله السوق |
| FR-5.5 | زر تواصل مباشر عبر واتساب |

### ٣.٦ إدارة المنصة

| الرقم | المتطلب |
|---|---|
| FR-6.1 | لوحة مالك المنصة: الشركات والاشتراكات وملف كامل لكل عميل |
| FR-6.2 | **تحرير الخطط من اللوحة**: السعر والعملة والحدود والمزايا والترتيب والعرض |
| FR-6.3 | طلبات التواصل الواردة |
| FR-6.4 | إخفاء الخطة لا حذفها — الحذف يكسر اشتراكات قائمة تشير إليها |
| FR-6.5 | تصدير بيانات كل الشركات إلى Excel |
| FR-6.6 | تفصيل التكلفة والهامش لكل عميل — مفتاح واحد يخدم الجميع، فالفاتورة تصل مجمّعة |
| FR-6.7 | مؤشرات الزوّار — تُقرأ بجلسة المالك لا بمفتاح الخدمة، وتقول «غير متاح» إن تعذّر القياس |

### ٣.٧ واتساب

| الرقم | المتطلب |
|---|---|
| FR-7.1 | ربط رقم الموظف بحسابه برمز يُولَّد داخل المنصة |
| FR-7.2 | الإجابة عبر واتساب من مستندات شركته وبصلاحيته هو |
| FR-7.3 | التحقق من توقيع النداء الوارد بمقارنة ثابتة الزمن |
| FR-7.4 | حدّ رسائل يومي لكل رقم |
| FR-7.5 | لا يُخدَم رقم غير موثَّق — `verified_at` شرط لا خيار |

### ٣.٨ الدعم الفني

| الرقم | المتطلب |
|---|---|
| FR-8.1 | تذاكر من الشركة إلى مالك المنصة، بحالة وأولوية |
| FR-8.2 | محادثة داخل التذكرة، ولا ينتحل العميل جهة المنصة |
| FR-8.3 | حالة التذكرة يغيّرها مالك المنصة وحده |
| FR-8.4 | تصدير التذاكر إلى Excel |

### ٣.٩ الاشتراك والدفع

| الرقم | المتطلب | التفصيل |
|---|---|---|
| FR-9.1 | شراء خطة عبر Moyasar | بالريال، تدعم مدى وApple Pay |
| FR-9.2 | **لا يُصدَّق العميل في أمر الدفع** | العودة من البوابة والـwebhook كلاهما إشارةٌ إلى أن نسأل، لا خبرٌ يُصدَّق |
| FR-9.3 | التحقق من الخادم بالمفتاح السري | ما تقوله واجهة Moyasar وحده يُكتب |
| FR-9.4 | مطابقة المبلغ والعملة | يُثبَّت المطلوب وقت الطلب ويُقارَن بما دُفع؛ وبدونه تُشترى خطة ٩٩٩ بريال |
| FR-9.5 | السعر يُقرأ من قاعدة البيانات لا من الطلب | — |
| FR-9.6 | التفعيل لا يتكرر | حارس `applied_at` مع `FOR UPDATE`؛ فحص الحالة وحده لا يكفي لأنها تبقى PAID بعد التطبيق |
| FR-9.7 | التمديد من نهاية الفترة القائمة | الاحتساب من الآن يبتلع أيامًا دُفع ثمنها |
| FR-9.8 | سجلّ المدفوعات لإدارة الشركة | لا لموظفيها |
| FR-9.9 | المنصة تعمل بلا بوابة | تظهر رسالة أن الاشتراك غير مفعّل، ولا يتعطّل شيء آخر |

### ٣.١٠ الصفحات القانونية

| الرقم | المتطلب |
|---|---|
| FR-10.1 | سياسة خصوصية وشروط استخدام بالعربية |
| FR-10.2 | الإفصاح عن المعالِجين الفرعيين ونطاق كلٍّ منهم |
| FR-10.3 | **لا تُختلق بيانات الكيان** — تُقرأ من البيئة، وتُعرض الصفحة مسوّدةً مع تنبيه ظاهر ما دامت ناقصة |

---

## ٤. المتطلبات غير الوظيفية

### ٤.١ الأمن — أهم قسم في هذه الوثيقة

| الرقم | المتطلب | التنفيذ |
|---|---|---|
| NFR-1.1 | **عزل تام بين الشركات** | RLS على كل جدول، والتحقق في قاعدة البيانات لا في الواجهة |
| NFR-1.2 | التحقق من الصلاحية على الخادم | كل إجراء يمرّ بـ`requirePermission` قبل أي عملية |
| NFR-1.3 | تقييد المستند داخل الشركة | لا تتسرّب مقاطع مستند ممنوع إلى سياق RAG |
| NFR-1.4 | لا أسرار في المتصفح | مفاتيح الخدمة على الخادم وحده |
| NFR-1.5 | لا أسرار في Git | `.env.example` بلا قيم |
| NFR-1.6 | لا تسجيل لمحتوى حسّاس | لا كلمات مرور ولا مفاتيح ولا محتوى وثائق |
| NFR-1.7 | لا تسريب أثر التنفيذ للمستخدم | رسائل عربية مفهومة؛ التفاصيل التقنية منقّحة من مسارات الخادم |
| NFR-1.8 | مسار التخزين يبدأ بمعرّف الشركة | وعليه تُبنى سياسات التخزين |
| NFR-1.9 | سجل تدقيق | لكل عملية حسّاسة |
| NFR-1.10 | تحديد المعدل | محادثة ٢٠/دقيقة، رفع ١٥/دقيقة، تعديلات ٦٠/دقيقة، زوّار ٦/دقيقة، **مصادقة ٨/٥ دقائق** |
| NFR-1.11 | ترويسات أمنية | HSTS، nosniff، منع التأطير، سياسة الإحالة |
| NFR-1.12 | **حدّ المصادقة على البريد والعنوان معًا** | الحدّ على أحدهما وحده يمرّ منه نمط هجوم واقعي: موزَّع على حساب واحد، أو كلمة واحدة على آلاف الحسابات |
| NFR-1.13 | حدّ الاستعادة يُطبَّق بصمت | تغيير الرسالة عند التجاوز يكشف أن البريد جُرّب كثيرًا |
| NFR-1.14 | لا تعداد للحسابات | «نسيت كلمة المرور» تُرجع النجاح دائمًا |
| NFR-1.15 | حماية من إعادة التوجيه المفتوح | وجهة `/auth/callback` داخلية فقط — و`//evil.com` يقرؤه المتصفح نطاقًا خارجيًا ويقرؤه الفحص الساذج مسارًا داخليًا |
| NFR-1.16 | التحقق من توقيع الـwebhooks | Moyasar وواتساب، بمقارنة ثابتة الزمن؛ وغياب الرمز يعني رفض الجميع لا قبولهم |
| NFR-1.17 | لا سياسة كتابة على المدفوعات | من يكتب صفّ دفع يمنح نفسه اشتراكًا |
| NFR-1.18 | دالة تفعيل الاشتراك محجوبة عن كل المستخدمين | لمفتاح الخدمة وحده |
| NFR-1.19 | حماية من حقن التعليمات | انظر ٣.٣.٢ |
| NFR-1.20 | القوائم البيضاء في SQL الديناميكي | `effective_plan_limit` تقصر المفتاح على المعروف |

#### التحقق — أرقام مقيسة لا مقدَّرة

| القياس | النتيجة |
|---|---|
| RLS على الجداول | **٢٥/٢٥** — مقروءة من كتالوج PostgreSQL لا من الكود |
| اختبارات العزل | **٩٩/٩٩ ناجحة** على PostgreSQL 16 حقيقية، بدور `authenticated` فتخضع لـRLS كأي طلب |
| الشاهد السلبي (mutation) | يُسقط **٣٢** اختبارًا عند تعطيل الحرّاس عمدًا |
| اختبارات الوحدة | **١٥٠ ناجحة**، ١٦ متخطّاة (تحتاج قاعدة حيّة) |
| هجوم استرجاع مباشر عبر الشركات | **٠ مقاطع** — بتضمين مقطع الشركة الأخرى وعتبة صفر وسقف ٢٠ |
| انتحال هوية في دالة الاسترجاع | `permission denied for function` |

**لماذا الشاهد السلبي:** اختبار ينجح في الحالتين لا قيمة له. تعطيل
الحرّاس عمدًا يثبت أن الاختبارات تكشف الخرق فعلًا ولا تنجح بالمصادفة.

#### الثغرات المفتوحة — مسجَّلة لا مخفيّة

| # | الثغرة | الخطورة | الأثر |
|---|---|---|---|
| G1 | تحديد المعدّل في ذاكرة كل نسخة | متوسط | الحدّ الفعلي يتضاعف بعدد نسخ Vercel. الواجهة مصمَّمة لاستبدال المخزن بـRedis بلا تغيير مواقع الاستدعاء |
| G2 | حقن التعليمات: مخاطرة متبقية | متوسط | لا دفاع قاطع في النماذج اللغوية. الضرر محصور داخل الشركة الواحدة |
| G3 | ١٦ اختبار تكامل متخطّى | متوسط | يحتاج مشروع Supabase للاختبار |
| G4 | لا حارس ثابت على `createAdminClient` | منخفض–متوسط | ملف جديد قد يستعمله بلا تحقّق ولا شيء يمنع |

**حالة حقن التعليمات بدقة:** «مُحصَّن ولم يُثبَت سلوكيًا» — لا «آمن».
التحصين مُختبَر إنشائيًا (١٢ اختبارًا على بناء النصّ)، ومختبر الهجوم على
النموذج الحيّ مكتوب (`npm run injection-drill`، ثمانية هجمات، وحكّامه
مضبوطون باختبارات) **ولم يُشغَّل بعدُ** لغياب مفتاح في بيئة التطوير.

### ٤.٢ الأداء

| الرقم | المتطلب |
|---|---|
| NFR-2.1 | زمن الإجابة المستهدف: أقل من ١٠ ثوانٍ للسؤال المعتاد |
| NFR-2.2 | معالجة مستند: ١٥–٦٠ ثانية حسب الحجم (سقف تنفيذ ٦٠ ثانية) |
| NFR-2.3 | بحث دلالي بفهرس HNSW لا بمسح كامل |

### ٤.٣ قابلية الاستخدام

| الرقم | المتطلب |
|---|---|
| NFR-3.1 | عربية أولًا، RTL أصلي لا معكوسًا بحيلة |
| NFR-3.2 | رسائل الخطأ تقول **ما العمل** لا ما حدث فقط |
| NFR-3.3 | صفحة `/setup` تشخّص التجهيز بالعربية مع روابط الإصلاح |
| NFR-3.4 | يعمل على الجوال |

### ٤.٤ الموثوقية

| الرقم | المتطلب | الحالة |
|---|---|---|
| NFR-4.1 | لا تعلق حالة المستند بلا سبب ظاهر | ✅ المعالجة تُنتظر ولا تُطلق في الخلفية |
| NFR-4.2 | فحص جاهزية حيّ `/api/health` | ✅ يفحص الاتصال فعلًا لا وجود المتغيّرات فقط |
| NFR-4.3 | نسخ احتياطي يومي | ⚠️ يتطلب ترقية Supabase إلى Pro |

---

## ٥. نموذج البيانات

٢٥ جدولًا موزّعة على سبع مجموعات:

| المجموعة | الجداول |
|---|---|
| **الأساس** | `companies`, `departments`, `profiles` |
| **المعرفة** | `knowledge_categories`, `documents`, `document_chunks` |
| **المحادثات** | `conversations`, `messages`, `message_sources`, `knowledge_gaps`, `knowledge_gap_askers` |
| **الفوترة** | `plans`, `subscriptions`, `payments`, `usage_records`, `ai_usage_logs` |
| **التشغيل** | `analytics_events`, `audit_logs`, `contact_requests`, `notifications` |
| **الدعم والقنوات** | `support_tickets`, `support_messages`, `whatsapp_links` |
| **الموقع** | `site_visitors`, `site_chat_messages` |

**ملاحظة تصميمية:** جدولا الزوّار **بلا عمود `company_id` أصلًا**، وبلا
أي سياسة كتابة. فخلط بيانات الزوّار ببيانات الشركات ممتنع بنيويًا لا
بسياسة قابلة للنسيان.

**ملاحظة ثانية:** الإجابة المعتمدة **مستند حقيقي** لا نوع بيانات جديد
(`source_kind = 'CURATED_ANSWER'`). فتسري عليها كل الضوابط المثبتة —
العزل والصلاحيات والبحث — بلا سياسة جديدة يمكن أن تُنسى. أضمن طريق لعدم
كسر العزل هو ألّا يُضاف إليه مسار جديد.

**ملاحظة ثالثة:** المبالغ تُخزَّن **بالهللة عددًا صحيحًا** لا بالريال
عشريًا. الحساب العشري يُدخل خطأ التقريب في المال، وهو آخر موضع يُحتمل
فيه.

---

## ٦. الواجهات الخارجية

| الخدمة | الغرض | أثر تعطّلها |
|---|---|---|
| Supabase | قاعدة البيانات والمصادقة والتخزين | النظام متوقف |
| Anthropic Claude | توليد الإجابات | المساعد معطّل، وباقي النظام يعمل |
| Voyage AI | التضمينات | لا رفع جديد ولا بحث دلالي |
| Vercel | الاستضافة | النظام متوقف |
| Resend (SMTP) | دعوات الموظفين واستعادة كلمة المرور | لا دعوات ولا استعادة؛ باقي النظام يعمل |
| Moyasar | تحصيل الاشتراكات | لا شراء جديد؛ الاشتراكات القائمة تعمل |
| WhatsApp Cloud API | قناة السؤال عبر واتساب | قناة واتساب معطّلة؛ المنصة تعمل |

---

## ٧. الخطط والحدود

| الخطة | السعر الشهري | المستخدمون | المستندات | الأسئلة/شهر | التخزين |
|---|---|---|---|---|---|
| **Starter** | ٤٩٩ ر.س | ٥٠ | ١٠٠ | ٥٬٠٠٠ | ٥ غ.ب |
| **Business** | ٩٩٩ ر.س | ٢٠٠ | ٥٠٠ | ٢٠٬٠٠٠ | ٢٥ غ.ب |
| **Enterprise** | تسعير مخصص | بلا حد | بلا حد | بلا حد | حسب الاتفاق |

الأسعار لا تشمل ضريبة القيمة المضافة.

**الحدود مفروضة تقنيًا** لا معروضة فقط: المستخدمون والمستندات والتخزين
والأسئلة الشهرية. والحقل الفارغ يعني «بلا حد»، **والصفر مرفوض صراحةً** —
لأنه يوقف كل استخدام، فخطأ قراءة واحد يعطّل شركة كاملة.

وثلاثة قرارات في العدّ تمسّ العميل مباشرةً:

- **المدعو يُحتسب مقعدًا** — وإلا أمكن تجاوز الحدّ بدعوات معلّقة تُفعَّل دفعةً
- **المؤرشف لا يُحتسب** — هو خارج قاعدة المعرفة، وعدّه يعاقب حسن التنظيم
- **التخزين يُفحص قبل الرفع** — الفحص بعده يترك ملفًا على حساب لم يشترِ مساحته

والحدود قابلة للتفاوض لكل عميل عبر `limit_overrides` في اشتراكه، دون
تغيير خطته ولا خطط غيره.

والخطط تُحرَّر من لوحة إدارة المنصة، ويظهر التغيير على صفحة الأسعار
فورًا — تُبطَل ذاكرة الصفحة بعد كل حفظ، وإلا ظنّ البائع أنه غيّر السعر
ورأى المشتري غيره.

---

## ٨. معايير القبول

يُعدّ النظام جاهزًا للعميل الأول حين تتحقق كل هذه:

- [x] شركة (أ) لا ترى أي بيانات لشركة (ب) — ٩٩ اختبارًا تُنفَّذ بدور المستخدم، وشاهد سلبي يُسقط ٣٢ منها عند تعطيل الحرّاس
- [x] مستند ممنوع لا تتسرّب مقاطعه إلى إجابة المساعد
- [x] بحث دلالي بتضمين مستند شركة أخرى يعيد **صفر** مقاطع
- [x] المساعد يقول «لا أعرف» بدل أن يخترع
- [x] لا مفاتيح في المتصفح ولا في المستودع
- [x] رسائل الفشل تسمّي السبب
- [x] دعوات البريد تصل، والموظف يضع كلمة مروره بنفسه
- [x] حدود الخطط مفروضة تقنيًا
- [x] بوابة دفع مبنيّة ومحميّة (مطابقة مبلغ، توقيع، تفعيل لا يتكرر)
- [x] صفحتا الخصوصية والشروط
- [ ] **تشغيل مختبر حقن التعليمات على النموذج الحيّ** ← `npm run injection-drill`
- [ ] تجربة دفع كاملة بمفاتيح Moyasar التجريبية
- [ ] رفع مستند عربي حقيقي على بيئة الإنتاج والتحقق من الإجابة
- [ ] نسخ احتياطي يومي مفعّل (Supabase Pro)
- [ ] نطاق مخصّص وتوثيقه في Resend
- [ ] بيانات الكيان النظامي في الصفحتين القانونيتين

---

## ٩. الاختبارات

| النوع | التغطية | العدد |
|---|---|---|
| اختبارات وحدة | التقطيع، التضمينات، الصلاحيات، معاملات النموذج، استخراج العربية، بيئة PDF، مفاتيح التخزين، معادلات التحقق واختيار السياق، **حقن التعليمات**، **حكّام مختبر الحقن**، **بوابة الدفع**، **وجهة ما بعد المصادقة** | ١٥٠ ناجحة |
| اختبارات عزل (SQL) | العزل بين الشركات، صلاحيات المستند، التنبيهات، واتساب، الدعم، التكلفة، **الفوترة والمدفوعات** | ٩٩/٩٩ |
| الشاهد السلبي | يُضعف الحرّاس عمدًا فتسقط ٣٢ اختبارًا | ✅ |
| حرّاس ثابتة | منع تمرير دالة من مكوّن خادم إلى مكوّن عميل؛ منع فهرس جزئي مُحكِّمًا لـON CONFLICT | ✅ |
| مختبر حقن على النموذج الحيّ | ٨ هجمات، حكّامه مضبوطون باختبارات | ⚠️ **مكتوب ولم يُشغَّل** |
| اختبارات تكامل | مكتوبة ومعطّلة — تحتاج مشروع Supabase للاختبار | ⚠️ ١٦ متخطّاة |

**الحرّاس الثابتة** أُضيفت بعد عطلين حقيقيين لم يمسكهما مترجم ولا مدقّق
أنواع: صفحة تُبنى بنجاح ثم تموت عند أول طلب، وفهرس يبدو سليمًا فيُفشل كل
إدراج بصمت. وكلاهما أُثبت بإعادة إدخال العطل والتأكد أن الحارس يسقط.

---

## ١٠. خارج النطاق الحالي

**هذا القسم هو أصدق ما في الوثيقة.** ما يلي غير مبنيّ:

| البند | الأثر على البيع |
|---|---|
| **الفاتورة الضريبية ١٥٪ (ZATCA)** | إلزامية نظامًا — **شرط قبل أول عميل يدفع** |
| **تشغيل مختبر حقن التعليمات** | التحصين غير مُثبَت سلوكيًا بعد |
| **OCR** | المستندات الممسوحة ضوئيًا (صور) غير مدعومة |
| **نسخ احتياطي يومي** | يتطلب Supabase Pro — **شرط قبل أي عميل يدفع** |
| **نطاق مخصص** | العنوان على `vercel.app` يضعف الثقة في البيع للشركات |
| **بيانات الكيان النظامي** | الصفحتان القانونيتان مسوّدة حتى تُضبط |
| **مراجعة نظامية للصفحتين** | ما فيهما صياغة تقنية لا استشارة قانونية |
| **تحديد معدّل موزَّع** | الحدّ الحالي لكل نسخة (انظر G1) |
| **شهادات امتثال** | لا SOC 2 ولا ISO — ستُطلب من العملاء الكبار |
| **تكاملات** | لا ربط بـ SharePoint أو Google Drive أو Slack |
| **تطبيق جوال** | الويب فقط (يعمل على الجوال) |
| **إلغاء/استرداد آلي** | الشروط تنصّ عليهما، والتنفيذ يدوي حاليًا |

---

## ١١. الأولويات التالية

| # | البند | لماذا الآن |
|---|---|---|
| ١ | تشغيل مختبر حقن التعليمات | آخر ادّعاء أمني غير مُثبَت — والإثبات بأمر واحد |
| ٢ | Supabase Pro (نسخ احتياطي) | فقدان بيانات عميل = نهاية المشروع |
| ٣ | تجربة الدفع بمفاتيح Moyasar التجريبية | لا تحتاج سجلًا تجاريًا، فلا سبب لتأجيلها |
| ٤ | الفاتورة الضريبية ١٥٪ | إلزامية نظامًا قبل أول تحصيل |
| ٥ | نطاق مخصص + توثيقه في Resend | مصداقية البيع، وإخراج البريد من مجلد المزعج |
| ٦ | بيانات الكيان النظامي ومراجعة الصفحتين | شرط تعاقدي |
| ٧ | تحديد معدّل موزَّع (Redis) | مع أول حمل حقيقي |
| ٨ | تشغيل اختبارات التكامل على مشروع حقيقي | آخر حلقة تحقق ناقصة |
